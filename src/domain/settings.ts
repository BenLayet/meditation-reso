export const defaultSettings = {
  preparationInSeconds: 20,
  durationInMinutes: 20,
  isGongOn: true,
  shouldDisplayProgress: true,
  shouldDisplayRemainingTime: true,
};
export type Settings = typeof defaultSettings;
